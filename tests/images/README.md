# Test images

Three representative sample photos are committed here so anyone can clone the
repo and test `analyze_meal_image` immediately (via `mcp dev`, the web UI, or a
real MCP client):

- `breakfast.jpg`
- `lunch.jpg`
- `salad.jpg`

Feel free to drop additional local images into this folder for your own manual
testing — anything beyond the three sample files above is gitignored, so your
own photos won't get committed.
